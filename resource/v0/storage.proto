syntax = "proto3";

option go_package = "github.com/n0stack/proto.go/resource/v0;presource";

package n0stack.resource;

message Storage {
  map<string, string> annotations = 1;
  
  uint64 bytes = 2;

  // 実際に使っているサイズ
  // uint64 allocated_bytes = 5;
}
